The script is really simple to run, simply download any decks you want to build from marvelcdb.com as .txt files, 
then place them in the card_files folder. then run the findDupes.py script via command line.
Two files should populate the directory that the script was run from
